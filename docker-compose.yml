version: "3.9"

services:
  frontend:
    build: .
    container_name: room-map-guru
    ports:
      - "8080:80"
    environment:
      - VITE_API_BASE_URL=
      - VITE_API_HOTEL_SEARCH=
      - VITE_API_HOTEL_SEARCH_TOKEN=